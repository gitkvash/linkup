package ge.kcamp.linkup.activity.enums;

/**
 * What a well-known place is, which is what the map draws inside its marker. Kept in step
 * with the CHECK constraint on {@code places.kind} (V31).
 */
public enum PlaceKind {
    LAKE,
    PARK,
    MALL,
    SPORTS,
    LANDMARK
}
